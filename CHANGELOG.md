# Changelog

형식은 [Keep a Changelog](https://keepachangelog.com/ko/1.1.0/), 버전은 [Semantic Versioning](https://semver.org/lang/ko/)을 따릅니다.

## [1.1.0] - 2026-09-17

### 추가
- **새 가족 만들기 잠금.** 사이트 주소는 공개라 누구나 열 수 있습니다. 새 가족 만들기는 기본으로 잠겨 있고,
  **맨 처음 만들어진 가족(주인)의 기기만** ⚙ 설정 → *새 가족 만들기* 에서 10분 동안 열 수 있습니다.
  열어둔 동안에만 다른 사람이 자기 가족을 만들 수 있고, 시간이 지나면 저절로 잠깁니다.
  나중에 초대받아 들어온 가족은 이 화면이 보이지 않아 또 다른 사람에게 열어줄 수 없습니다.
  이미 있는 가족과 *가족 코드로 참여*는 영향받지 않습니다.

  새로 설치하면 첫 가족이 그냥 만들어지고 그 가족이 자동으로 주인이 됩니다.
  **이미 쓰고 있던 프로젝트라면** SQL Editor에서 `schema.sql`의 잠금 부분을 실행한 뒤 주인을 지정합니다:
  ```sql
  update app_gate
     set owner_family_id = (select id from families order by created_at asc limit 1)
   where id = 1;
  ```

### 변경
- **아이 색을 아이들이 부르는 그 색으로.** 핫핑크·샛노랑·초록·파랑처럼 채도를 살렸고, 선택된 탭의 밑줄을 3px → 4px로.
  아이가 "내 색"을 바로 알아보는 게 이 색의 유일한 목적이라 또렷한 쪽이 맞습니다.
  (노랑만은 글자로 쓰면 어둡게 눌러야 해서 금색에 가깝습니다. 밑줄과 점은 샛노랑 그대로입니다.)
- 아이에게 보여주는 화면의 지폐를 `1만` → `10000` 처럼 숫자 그대로. 아이가 `1만`의 1을 보고
  500보다 작다고 읽는 일이 있었습니다. 자릿수가 많은 쪽이 큰 돈이라는 게 바로 보이도록 했습니다.
  지폐가 조금 커졌고, 줄이 많거나 화면이 작으면 돼지 그림을 줄여 한 화면에 담습니다.
- **날짜 고르기를 앱 안에서.** 기기 기본 달력(`input type=date`)이 열리면 그 화면만 OS 톤이라 튀었습니다.
  같은 문법으로 달력을 직접 그렸습니다. 오늘 이후는 고를 수 없고, 고치기 화면의 날짜 칸도 같은 달력을 씁니다.
- manifest `background_color`를 흰색(`#FDFCFA`)으로. 크림으로 두면 안드로이드 실행 화면에서
  시스템이 아이콘 뒤에 그리는 흰 판이 드러나 더 지저분해집니다.

### 수정
- 오랜만에 앱을 열 때 잠깐 떴다 사라지던 오류 알림. 만료된 토큰 갱신과 첫 요청이 겹쳐 한 번 튕기는 것이라,
  이제 조용히 한 번 더 시도하고 그래도 실패할 때만 알립니다.
- **연결이 안 될 때 기록이 없는 것처럼 보이던 문제.** 서버에 못 닿으면 *새로 시작하기* 화면이나
  "설정에서 아이를 먼저 추가해 주세요"가 떴습니다. 이제 마지막으로 받은 내용을 그대로 보여주고
  위에 `연결이 안 돼요` 한 줄이 남습니다(눌러서 다시 시도). 쓰던 내용이 없는 기기에서는
  시작 화면 대신 연결 실패 화면이 나옵니다.
- **매주 용돈이 1년쯤 뒤 조용히 멈추던 문제.** 자동 입금을 시작일부터 하루씩 세느라 400일 안전장치에
  먼저 걸려, 그 뒤로는 최근 주가 영영 채워지지 않았습니다. 이제 마지막 자동 줄 다음 날부터 셉니다.
  (아주 오래 안 열었다면 최근 400일까지만 채웁니다 — 몇 년치가 한꺼번에 들어오지 않도록)
- **기록이 1000줄을 넘으면 잔액이 틀려지던 문제.** 서버가 한 번에 주는 최대 줄 수가 1000이라
  그 이상은 조용히 잘렸습니다. 이제 나눠서 전부 받아옵니다.
- 내보내기에서 공유·복사가 모두 안 되는 환경일 때 아무 일도 일어나지 않던 것, 가족 코드가 겹쳤을 때
  데이터베이스 오류 문구가 그대로 보이던 것.

## [1.0.2] - 2026-09-16

### 수정
- **안드로이드 뒤로 가기.** 설정 화면이나 시트를 열고 뒤로 가기를 누르면 앱이 종료됐습니다.
  이제 열려 있는 화면이 하나씩 닫히고, 다 닫힌 뒤에 눌러야 앱을 벗어납니다.
  (iOS 홈 화면 앱에는 뒤로 가기가 없어 왼쪽 가장자리 스와이프가 그 역할을 합니다)

### 변경
- `mobile-web-app-capable` 메타 태그 추가 (크롬 경고)

## [1.0.1] - 2026-09-16

### 변경
- 시작 화면 로고를 배경 없는 그림으로 (흰 바탕 위에 크림색 사각형이 보이던 문제)
- 설정 화면의 버전 표기를 `v1.0.0` 형식으로

### 추가
- iOS 실행 화면(`splash.png`). 홈 화면 앱을 열 때 흰 바탕에 아이콘이 뜨는 대신 크림 바탕에 로고가 나옵니다.
  iOS는 이 이미지를 설치 시점에 저장하므로, 홈 화면 앱을 지우고 다시 추가해야 반영됩니다.

## [1.0.0] - 2026-09-16

첫 정식 버전. 이전의 v6~v14 내부 버전을 정리한 것으로, 그 사이에 DB 스키마 변경은 없습니다.

### 기능
- 아이별 통장 — 날짜 · 용도 · 금액 · 누적 총액, 월 구분선과 월 합계, 60줄 단위 더 보기
- 처음 금액 줄 (통장 맨 아래, 날짜 포함)
- 매주 용돈 자동 입금 — 요일·금액, 한 주 건너뛰기, 날짜별 중복 방지
- 아이에게 보여주기 — 잔액을 누르면 돼지저금통과 지폐·동전 그림
- 가족 코드로 기기 연결, 익명 로그인, 실시간 동기화
- 기록 텍스트 내보내기
- 서버 설정이 없으면 로컬 모드

### 화면
- 아이 색 6종 (파랑 · 초록 · 노랑 · 주황 · 분홍 · 보라)
- 설정 화면: 아이 / 매주 용돈 / 동기화 / 기록. 아이 지우기(2단계 확인), 매주 용돈은 요약 행 → 시트
- 설정 화면에서 왼쪽 가장자리 스와이프로 뒤로
- 홈 화면 아이콘: 돼지저금통 (`logo/logo-master.png`에서 생성)

### 업그레이드
- v5 이하에서 올라오는 경우 SQL Editor에서 한 번:
  `alter table entries add column if not exists skipped boolean not null default false;`
- v11 이하에서 올라오는 경우 아이콘이 바뀌었으므로 홈 화면 앱을 지우고 다시 추가
