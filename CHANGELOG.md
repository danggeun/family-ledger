# Changelog

## 1.1.3 — 2026-09-21

- 최근 용도 칩이 많이 쓴 순으로. 칩을 누르면 받은 돈/쓴 돈도 그 용도에 맞게 같이 바뀜
- 아이 이름을 크게, 아이가 고른 색 그대로 (홈 탭과 아이 화면 같은 색)
- 기록 한 줄을 길게 누르면 그 금액이 지폐·동전 그림으로. 나간 돈은 옅은 점선으로 "빠진 자리"
- 같은 지폐가 다섯 장을 넘으면 한 장 + `×7` 로 (어떤 금액에서도 줄이 안 잘림)
- 설정의 `＋ 아이 추가` 를 맨 아래 버전과 같은 줄·같은 톤으로
- 적다 만 입력은 아이를 바꾸거나 입력줄 밖으로 나가면 비워짐 (금액까지 넣었으면 남음).
  적는 중에 밖을 누르면 키보드만 내려가고, 누른 곳이 바로 열리지는 않음
- 아이에게 보여주는 화면에서 좌우로 밀면 다른 아이 돈 (닫으면 원래 아이로)
- 아이를 추가하면 알려주는 문구
- 불러오는 중 문구가 0.35초 안에 끝나면 아예 안 뜨고, 느릴 때만 서서히 나타남
- 고치기 시트의 "내가 적었어요"가 마지막에 손댄 사람 기준으로. 고친 줄은 "고쳤어요"
  (쓰던 프로젝트라면 `alter table entries add column if not exists updated_by uuid default auth.uid();`)
- 자정~아침 사이에 추가한 아이의 `처음 금액` 줄 날짜가 하루 전으로 나오던 문제

## 1.1.2 — 2026-09-17

- 설정 순서: 기록 → 초대
- 최근 용도·날짜 칩이 입력줄 밖을 누르면 닫힘
- 시트를 아래로 밀어서 닫기
- 고치기 시트에서도 받은 돈/쓴 돈이 목록과 같은 색으로 구분됨
- 기록에 적은 기기를 남기고, 고치기 시트에 한 줄로 표시
  (쓰던 프로젝트라면 `alter table entries add column if not exists created_by uuid default auth.uid();`)

## 1.1.1 — 2026-09-17

- 달력에서 날짜를 누르면 표시만 되고, 확인을 눌러야 닫힘
- 날짜 칩에 고른 날짜를 그대로 표시
- 설정 문구: 앱 나눠주기 → 초대 / 잠깐 열어두기

## 1.1.0 — 2026-09-17

- 초대 — 다른 사람이 새로 시작하는 것을 잠그고, 처음 만든 가족만 10분씩 열 수 있음
  (쓰던 프로젝트라면 `schema.sql`의 `app_gate` 부분 실행 후
  `update app_gate set owner_family_id = (select id from families order by created_at limit 1) where id = 1;`)
- 아이 색을 더 또렷하게, 탭 밑줄 4px
- 아이 화면 지폐를 `10000` 처럼 숫자로. 줄이 많으면 돼지를 줄여 한 화면에
- 날짜 고르기를 앱 안 달력으로 (기기 기본 달력 제거)
- 연결이 안 될 때 마지막으로 받은 내용 + 안내. 시작 화면이 뜨지 않음
- 매주 용돈이 1년쯤 뒤 멈추던 문제 (400일 한계)
- 기록이 1000줄을 넘으면 잔액이 틀려지던 문제
- 토큰 만료로 뜨던 오류 알림, 내보내기 폴백, 가족 코드 충돌 문구

## 1.0.2 — 2026-09-16

- 안드로이드 뒤로 가기로 화면이 하나씩 닫힘 (앱이 꺼지던 문제)

## 1.0.1 — 2026-09-16

- 시작 화면 로고를 배경 없는 그림으로
- iOS 실행 화면 `splash.png`

## 1.0.0 — 2026-09-16

첫 버전.
